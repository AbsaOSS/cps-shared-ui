import { CpsTableColumnFilterComponent } from './cps-table-column-filter.component';

describe('CpsTableColumnFilterComponent', () => {
  it('should mount', () => {
    cy.mount(CpsTableColumnFilterComponent);
  });
});
