import { TableColumnFilterConstraintComponent } from './table-column-filter-constraint.component';

describe('TableColumnFilterConstraintComponent', () => {
  it('should mount', () => {
    cy.mount(TableColumnFilterConstraintComponent);
  });
});
