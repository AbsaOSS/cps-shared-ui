import { TableColumnFilterComponent } from './table-column-filter.component';

describe('TableColumnFilterComponent', () => {
  it('should mount', () => {
    cy.mount(TableColumnFilterComponent);
  });
});
